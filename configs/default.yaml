data:
  seed: 42
  test_size: 0.15
  val_size: 0.15

model:
  lr: 0.01
  max_iter: 1000
  tol: 1e-4
  batch_size: 64
  method: "sgd"

paths:
  model: "artifacts/model.joblib"
  scaler: "artifacts/scaler.joblib"
  metrics: "artifacts/metrics.json"